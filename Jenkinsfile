pipeline {
    agent any

    stages {
        stage('Dev Dummmy code') {
            steps {
                echo 'Building the code.............'
                sh "echo Building the code.............2 "
                sh "hostname"
                sh "uptime"
            }
        }
    }
}
